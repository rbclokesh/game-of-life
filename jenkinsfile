node {

   stage('SCM') {
      // git clone	 
     	 https://github.com/rbclokesh/game-of-life.git
   }
   
   stage ('build the packages') {
      // mvn package
	  sh 'mvn clean package'
   }  
   stage ('archival') {
     // archiving artifacts
	 archive 'target/*.jar'
   }

   
}
